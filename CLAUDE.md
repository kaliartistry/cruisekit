@AGENTS.md

# CruiseKit Web Handoff

Before planning or changing this repository, read `docs/agent-handoff.md`.

GitHub is the source of truth. Do not infer current release state from a prior Claude Code or Codex conversation. Confirm the branch, remote state, open pull requests, and live deployment first.

When work changes a deployment, release status, architecture decision, cross-repository dependency, or known blocker, update `docs/agent-handoff.md` in the same pull request.
